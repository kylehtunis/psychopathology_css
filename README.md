# Readme

Model and network code located in `./src/` and can be imported as a module. Analyses, figures, and animations generated by `project.ipynb`.

## Example usage

```python
G = get_symptom_network()
model = IsingSimulation(G)
model.run()
```